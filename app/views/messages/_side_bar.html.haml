.side-bar
  .side-header
    .side-header__box
      %p.side-header__box__username
        =current_user.name
      %ul.side-header__box__menu
        %li
          = link_to new_group_path, class: 'side-header__box__menu__new-group' do
            = icon('far', 'edit')
        %li
          = link_to edit_user_path(current_user), class: 'sideHeader__box__menu__edit-user' do
            = icon('fas', 'cog')
  .groups
    - current_user.groups.each do |group|  
      = link_to '#', class: 'group' do
        %p.group__group-name
          = group.name
        %p.group__latest-message
          メッセージはまだありません